{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rimraf src/ && babel ./ --out-dir src/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files",
    "start-docker-dev": "NODE_ENV=docker nodemon --exec babel-node src/index.js",
    "start-docker-prod": "NODE_ENV=prod npm run build && node src/index.js",
    "start-dev": "NODE_ENV=prod nodemon --exec babel-node src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.13.15",
    "@babel/node": "^7.13.13",
    "@babel/preset-env": "^7.13.15",
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "nodemon": "^2.0.7",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mqtt": "^4.2.6",
    "node-wav": "0.0.2",
    "socket.io": "^4.0.1",
    "socket.io-stream": "^0.9.1"
  }
}
